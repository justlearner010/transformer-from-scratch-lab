# Exploration Lane Design

主学习线保持固定：材料链接、最小任务、正式证据、提交、状态转换。探索线允许学生
在任意 Gate 提问、提出假设或做自主实验；AI 可以解释、追问和建议最小验证，但不
写入状态、不替代正式证据、不自动改写学生答案。

探索内容只有在学生主动请求“转为证据”时，才由规则检查它是否匹配当前 Gate 已发布
的 criterion；不匹配则保留为探索笔记。未来命令边界为 `/ask`、`/return` 与
`/convert`，但本文件只记录设计，不启用新的状态入口。
