# Week 0 工程性作业：完成 Lab 后

每题都必须引用你自己的 Lab 输入、grader 反馈或 mini demo 结果；不要只给抽象定义。

1. 选择一次 `require_matrix` 拒绝的输入。它违反了哪个边界？如果不在这里拒绝，它最晚会在哪一个后续函数使错误更难定位？
2. 用你的 token 表与投影矩阵解释一次兼容的矩阵乘法：分别写出两个输入和输出的 shape，并说明关卡 3 为什么只检查内维度。
3. 选择你在 `dot_entry` 中计算的一个位置。解释它对应 left 的哪一行、right 的哪一列；若把右矩阵的“列”误写成“行”，你预计会出现什么现象？
4. 比较 `matmul_from_entries` 与 NumPy `@` 的定位角色：当二者不一致时，你会先检查哪两个函数，为什么不先怀疑整个矩阵？
5. 展示一次不兼容输入的 `describe_product` 输出。它怎样比原始的矩阵乘法异常更有助于定位？请写出你下一步会检查的具体对象。
