"""Micro-lab 3: read values through fixed attention weights."""
import numpy as np


def read_values(weights: np.ndarray, values: np.ndarray) -> np.ndarray:
    """Return weights @ values; reject incompatible row counts."""
    raise NotImplementedError("完成 value read：区分 weights 的匹配职责和 V 的内容职责")
