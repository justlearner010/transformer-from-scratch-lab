# Gate 1–5：Week 0 概念练习（先于 Lab）

## 概念本身

1. 给出 `(3,2) @ (2,2)` 的结果 shape，并手算结果矩阵的一个元素。
2. 对 `[2,1,0]` 手算 softmax；证明该行结果和为 1。

## 使用边界

3. 构造一对不能相乘的矩阵，指出不匹配的两个维度。
4. 说明直接计算 `exp([1000,999,998])` 的问题，并写出稳定 softmax 的计算顺序。

## 全局地位

5. 用 150–200 字说明“矩阵投影、点积、softmax”会分别在 Week 1 attention 的哪一环出现；若省略其中一个，信息流为什么无法继续。

**进入 Lab：** 五题均完成，且第 5 题不引用教材原句；若无法闭卷说清
输入、输出与 shape，回到对应 Gate，而不是直接运行 Lab。
