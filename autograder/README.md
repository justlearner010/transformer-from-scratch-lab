# 隐藏评分器说明

公开仓库无法安全保存 hidden tests：任何已提交的测试都能被查看。因此真正的 `.grader/` 目录只保留在学习者本地，且由根目录 `.gitignore` 排除。

评分器必须只返回：

- 当前关卡；
- 通过项数量；
- 失败类别（数值稳定性、shape/维度、公式、边界输入）；
- 一个思考提示；
- 下一步是否解锁。

评分器不得打印隐藏输入、期望矩阵、断言源码或参考实现。
