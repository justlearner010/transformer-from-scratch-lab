# 小习题

请先手算，再用你自己的 NumPy 实现核对；不要查 Lab 或参考实现。

1. 令 `X` 的 shape 为 `(3, 4)`，`W_Q`、`W_K`、`W_V` 的 shape 都为 `(4, 2)`。写出 Q、K、V、`QK^T`、attention weights 和输出的 shape。
2. 对分数行 `[2, 1, 0]` 手算 softmax，验证结果之和为 1。
3. 将上一题每个数同时加上 100，说明 softmax 输出为何不变。
4. 设 `d_k = 64`。解释若省略缩放，softmax 输入会出现什么趋势；这对梯度意味着什么？
5. 写一个两 token 的例子，使第一个 token 更偏向读取自己；解释 Q、K、V 分别应具备什么关系。

## 自检

写完后才能查看 `lab/tests/`。公开测试通过不代表本题完成；你必须能解释每一步。
